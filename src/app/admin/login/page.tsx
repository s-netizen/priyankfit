export default function AdminLogin() {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: "var(--dark)" }}>
      <div className="card p-8 text-center max-w-sm w-full">
        <p className="text-white mb-2" style={{ fontFamily: "var(--font-display)", fontSize: "1.5rem" }}>
          Priyank<span style={{ color: "var(--red)" }}>Fit</span>
        </p>
        <p className="text-gray-500 text-sm">Admin panel coming soon — backend setup in progress.</p>
      </div>
    </div>
  );
}
